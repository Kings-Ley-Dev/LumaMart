'use client'
import Image from 'next/image'; import { MapPin, Mail, Phone } from 'lucide-react'
export default function StoreInfo({store}){return <div className="flex-1 min-w-0 text-sm"><div className="flex flex-col sm:flex-row gap-4"><Image width={64} height={64} src={store.logo} alt={store.name} className="size-16 rounded-2xl object-cover border border-slate-100"/><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h3 className="text-xl font-semibold text-slate-900">{store.name}</h3><span className="text-xs text-slate-400">@{store.username}</span><span className={`rounded-full px-2.5 py-1 text-[10px] font-semibold uppercase ${store.status==='pending'?'bg-amber-50 text-amber-700':store.status==='rejected'?'bg-red-50 text-red-700':'bg-emerald-50 text-emerald-700'}`}>{store.status}</span></div><p className="mt-2 max-w-2xl leading-6 text-slate-500">{store.description}</p></div></div><div className="mt-5 grid sm:grid-cols-3 gap-3 text-slate-500"><p className="flex gap-2"><MapPin size={15} className="shrink-0 mt-0.5"/>{store.address}</p><p className="flex gap-2"><Phone size={15} className="shrink-0 mt-0.5"/>{store.contact}</p><p className="flex gap-2"><Mail size={15} className="shrink-0 mt-0.5"/>{store.email}</p></div></div>}
