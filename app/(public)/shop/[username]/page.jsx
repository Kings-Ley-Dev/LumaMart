'use client'
import ProductCard from '@/components/ProductCard'; import { dummyStoreData, productDummyData } from '@/assets/assets'; import { MapPin, Mail, Store } from 'lucide-react'; import Image from 'next/image'; import { useParams } from 'next/navigation'
export default function StoreShop(){useParams();const store=dummyStoreData;return <main className="page-shell py-10 sm:py-14 min-h-[65vh]"><section className="overflow-hidden rounded-[32px] bg-slate-950 text-white p-6 sm:p-10"><div className="flex flex-col md:flex-row gap-6 items-start md:items-center"><Image src={store.logo} alt={store.name} width={100} height={100} className="size-24 rounded-3xl"/><div className="flex-1"><div className="flex items-center gap-2 text-xs text-violet-300"><Store size={14}/> Independent store</div><h1 className="mt-2 text-3xl sm:text-4xl font-semibold">{store.name}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">{store.description}</p><div className="mt-4 flex flex-wrap gap-x-5 gap-y-2 text-xs text-slate-400"><span className="flex items-center gap-1"><MapPin size={14}/>{store.address}</span><span className="flex items-center gap-1"><Mail size={14}/>{store.email}</span></div></div></div></section><section className="mt-12"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-violet-600">Storefront</p><h2 className="mt-1 text-2xl sm:text-3xl font-semibold">Shop {store.name}</h2></div><div className="mt-7 grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-x-4 sm:gap-x-5 gap-y-9">{productDummyData.map(p=><ProductCard key={p.id} product={{...p,store}}/>)}</div></section></main>}
