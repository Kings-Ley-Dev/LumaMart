'use client'
import { Suspense, useMemo, useState } from 'react'; import ProductCard from '@/components/ProductCard'; import { Search, SlidersHorizontal, X } from 'lucide-react'; import { useSearchParams } from 'next/navigation'; import { useSelector } from 'react-redux'; import Link from 'next/link'
function ShopContent(){const params=useSearchParams();const initial=params.get('search')||'';const [query,setQuery]=useState(initial);const [category,setCategory]=useState('All');const products=useSelector(s=>s.product.list);const cats=['All',...new Set(products.map(p=>p.category))];const filtered=useMemo(()=>products.filter(p=>(!query||`${p.name} ${p.category}`.toLowerCase().includes(query.toLowerCase()))&&(category==='All'||p.category===category)),[products,query,category]);return <main className="page-shell py-10 sm:py-14 min-h-[65vh]"><div className="flex flex-col lg:flex-row lg:items-end justify-between gap-5"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-violet-600">Catalog</p><h1 className="mt-1 text-3xl sm:text-4xl font-semibold">Browse products</h1><p className="mt-2 text-sm text-slate-500">{filtered.length} products match your selection.</p></div><div className="flex w-full lg:w-auto items-center rounded-xl border border-slate-200 bg-white px-4 py-3 lg:min-w-80 focus-within:ring-2 focus-within:ring-violet-200"><Search size={17} className="text-slate-400"/><input value={query} onChange={e=>setQuery(e.target.value)} className="w-full outline-none pl-2 text-sm" placeholder="Search by product or category"/>{query&&<button onClick={()=>setQuery('')}><X size={16}/></button>}</div></div><div className="mt-7 flex items-center gap-2 overflow-x-auto no-scrollbar pb-1"><SlidersHorizontal size={17} className="text-slate-400 shrink-0"/>{cats.map(c=><button key={c} onClick={()=>setCategory(c)} className={`shrink-0 rounded-full px-4 py-2 text-xs font-semibold ${category===c?'bg-slate-950 text-white':'border border-slate-200 text-slate-500 hover:border-violet-300 hover:text-violet-600'}`}>{c}</button>)}</div>{filtered.length?<div className="mt-8 grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-x-4 sm:gap-x-5 gap-y-9">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="mt-10 rounded-2xl border border-dashed border-slate-300 p-14 text-center"><p className="font-semibold">No products found</p><p className="mt-1 text-sm text-slate-500">Try a different search or category.</p><Link href="/shop" onClick={()=>{setQuery('');setCategory('All')}} className="mt-5 inline-flex rounded-xl bg-slate-950 px-5 py-2.5 text-sm font-semibold text-white">Reset filters</Link></div>}</main>}
export default function Shop(){return <Suspense fallback={<main className="page-shell py-20 text-sm text-slate-400">Loading catalog...</main>}><ShopContent/></Suspense>}
