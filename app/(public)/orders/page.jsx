'use client'
import { orderDummyData } from '@/assets/assets'; import { Package, ChevronRight } from 'lucide-react'; import Image from 'next/image'; import Link from 'next/link'
const statusLabel=s=>s.replaceAll('_',' ').toLowerCase().replace(/\b\w/g,c=>c.toUpperCase())
export default function Orders(){return <main className="page-shell py-12 sm:py-16 min-h-[65vh]"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-violet-600">Account</p><h1 className="mt-1 text-3xl font-semibold">Order history</h1><p className="mt-2 text-sm text-slate-500">{orderDummyData.length} recent orders in this demo.</p></div><div className="mt-7 space-y-4">{orderDummyData.map(o=><article key={o.id} className="rounded-2xl border border-slate-200 bg-white p-4 sm:p-6"><div className="flex flex-col sm:flex-row sm:items-center justify-between gap-3 pb-4 border-b border-slate-100"><div><p className="font-semibold">Order #{o.id.slice(-6)}</p><p className="mt-1 text-xs text-slate-400">{new Date(o.createdAt).toLocaleString()}</p></div><span className="w-fit rounded-full bg-emerald-50 px-3 py-1 text-xs font-semibold text-emerald-700">{statusLabel(o.status)}</span></div><div className="mt-4 grid gap-3">{o.orderItems.map((item,i)=><div key={i} className="flex items-center gap-3"><Image src={item.product.images[0]} alt="" width={56} height={56} className="size-14 rounded-xl bg-slate-100 object-cover"/><div className="min-w-0 flex-1"><p className="text-sm font-medium truncate">{item.product.name}</p><p className="text-xs text-slate-400">Qty {item.quantity}</p></div><p className="text-sm font-semibold">${item.price}</p></div>)}</div><div className="mt-5 pt-4 border-t border-slate-100 flex flex-wrap items-center justify-between gap-3 text-sm"><span className="text-slate-500">{o.paymentMethod} · {o.address.city}</span><span className="font-semibold">Total ${o.total}</span></div></article>)}<Link href="/shop" className="inline-flex items-center gap-2 rounded-xl bg-slate-950 text-white px-5 py-3 text-sm font-semibold">Continue shopping <ChevronRight size={16}/></Link></div></main>}
