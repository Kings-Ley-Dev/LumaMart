'use client'
import ProductDescription from '@/components/ProductDescription'; import ProductDetails from '@/components/ProductDetails'; import { useParams } from 'next/navigation'; import { useEffect,useState } from 'react'; import { useSelector } from 'react-redux'; import Link from 'next/link'; import { ChevronRight } from 'lucide-react'
export default function Product(){const {productId}=useParams();const products=useSelector(s=>s.product.list);const [product,setProduct]=useState(null);useEffect(()=>{setProduct(products.find(p=>p.id===productId));window.scrollTo(0,0)},[productId,products]);if(!product)return <main className="page-shell py-24 text-center"><h1 className="text-2xl font-semibold">Product not found</h1><Link href="/shop" className="mt-5 inline-flex items-center gap-2 text-violet-600 text-sm font-semibold">Back to shop <ChevronRight size={15}/></Link></main>;return <main className="page-shell py-8 sm:py-12"><div className="flex items-center gap-1 text-xs text-slate-400 mb-6"><Link href="/">Home</Link><ChevronRight size={13}/><Link href="/shop">Shop</Link><ChevronRight size={13}/><span>{product.category}</span></div><ProductDetails product={product}/><ProductDescription product={product}/></main>}
