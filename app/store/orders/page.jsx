'use client'
import { orderDummyData } from '@/assets/assets'; import { useState } from 'react'; import toast from 'react-hot-toast'; import { X } from 'lucide-react'; import Image from 'next/image'
const statuses=['ORDER_PLACED','PROCESSING','SHIPPED','DELIVERED']
export default function StoreOrders(){const [orders,setOrders]=useState(orderDummyData);const [selected,setSelected]=useState(null);const update=(id,status)=>{setOrders(os=>os.map(o=>o.id===id?{...o,status}:o));toast.success('Order status updated')};return <div><p className="text-xs font-semibold uppercase tracking-[.2em] text-violet-600">Sales</p><h1 className="mt-1 text-3xl font-semibold">Orders</h1><p className="mt-2 text-sm text-slate-500">Review purchases and move them through fulfillment.</p><div className="mt-7 rounded-2xl border border-slate-200 bg-white overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-sm text-left"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-500"><tr>{['Order','Customer','Total','Payment','Status','Date'].map(h=><th key={h} className="px-5 py-4">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{orders.map(o=><tr key={o.id} onClick={()=>setSelected(o)} className="hover:bg-slate-50 cursor-pointer"><td className="px-5 py-4 font-medium">#{o.id.slice(-6)}</td><td className="px-5 py-4">{o.user?.name}</td><td className="px-5 py-4 font-semibold">${o.total}</td><td className="px-5 py-4 text-slate-500">{o.paymentMethod}</td><td className="px-5 py-4" onClick={e=>e.stopPropagation()}><select value={o.status} onChange={e=>update(o.id,e.target.value)} className="rounded-lg border border-slate-200 px-2 py-1.5 text-xs"><option>{statuses[0]}</option><option>{statuses[1]}</option><option>{statuses[2]}</option><option>{statuses[3]}</option></select></td><td className="px-5 py-4 text-xs text-slate-400">{new Date(o.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div></div>{selected&&<div className="fixed inset-0 z-50 bg-slate-950/40 backdrop-blur-sm p-4 grid place-items-center" onClick={()=>setSelected(null)}><div className="w-full max-w-2xl max-h-[90vh] overflow-y-auto rounded-3xl bg-white p-6 sm:p-8" onClick={e=>e.stopPropagation()}><div className="flex justify-between items-start"><div><p className="text-xs text-violet-600 font-semibold uppercase tracking-[.2em]">Order #{selected.id.slice(-6)}</p><h2 className="mt-1 text-2xl font-semibold">Order details</h2></div><button onClick={()=>setSelected(null)} className="rounded-full bg-slate-100 p-2"><X size={18}/></button></div><div className="mt-6 grid sm:grid-cols-2 gap-4 text-sm"><div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs text-slate-400">Customer</p><p className="mt-1 font-medium">{selected.user?.name}</p><p className="text-slate-500">{selected.user?.email}</p></div><div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs text-slate-400">Delivery</p><p className="mt-1 text-slate-600">{selected.address?.street}, {selected.address?.city}, {selected.address?.state}</p></div></div><div className="mt-6 space-y-3">{selected.orderItems.map((item,i)=><div key={i} className="flex items-center gap-3 border border-slate-100 rounded-2xl p-3"><Image src={item.product.images[0]} alt="" width={56} height={56} className="size-14 rounded-xl object-cover bg-slate-100"/><div className="flex-1"><p className="font-medium">{item.product.name}</p><p className="text-xs text-slate-400">Qty {item.quantity}</p></div><p className="font-semibold">${item.price}</p></div>)}</div></div></div>}</div>}
