'use client'
import { storesDummyData } from '@/assets/assets'; import StoreInfo from '@/components/admin/StoreInfo'; import { useState } from 'react'; import toast from 'react-hot-toast'
export default function AdminStores(){const [stores,setStores]=useState(storesDummyData);const toggle=id=>{setStores(s=>s.map(x=>x.id===id?{...x,isActive:!x.isActive}:x));toast.success('Store status updated')};return <div><p className="text-xs font-semibold uppercase tracking-[.2em] text-violet-600">Marketplace</p><h1 className="mt-1 text-3xl font-semibold">Live stores</h1><p className="mt-2 text-sm text-slate-500">Manage storefront visibility and status.</p><div className="mt-7 grid gap-4">{stores.map(store=><div key={store.id} className="rounded-2xl border border-slate-200 bg-white p-5 sm:p-6"><StoreInfo store={store}/><div className="mt-5 flex items-center gap-3"><button onClick={()=>toggle(store.id)} className={`relative h-7 w-12 rounded-full transition ${store.isActive?'bg-violet-600':'bg-slate-300'}`} aria-label="Toggle store"><span className={`absolute top-1 size-5 rounded-full bg-white transition ${store.isActive?'left-6':'left-1'}`}/></button><span className="text-sm font-medium">{store.isActive?'Visible to shoppers':'Hidden from shoppers'}</span></div></div>)}</div></div>}
