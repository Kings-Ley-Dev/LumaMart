'use client'
import { couponDummyData } from '@/assets/assets'; import { useState } from 'react'; import { Trash2, Plus } from 'lucide-react'; import toast from 'react-hot-toast'
export default function AdminCoupons(){const [coupons,setCoupons]=useState(couponDummyData);const [form,setForm]=useState({code:'',description:'',discount:'',expiresAt:''});const add=e=>{e.preventDefault();const c={...form,discount:Number(form.discount),forNewUser:false,forMember:false,isPublic:true};setCoupons(x=>[...x,c]);setForm({code:'',description:'',discount:'',expiresAt:''});toast.success('Coupon added')};return <div><p className="text-xs font-semibold uppercase tracking-[.2em] text-violet-600">Promotions</p><h1 className="mt-1 text-3xl font-semibold">Coupons</h1><div className="mt-7 grid lg:grid-cols-[380px_1fr] gap-5 items-start"><form onSubmit={add} className="rounded-2xl border border-slate-200 bg-white p-6 space-y-4"><h2 className="font-semibold">Create coupon</h2>{[['code','Code','text'],['description','Description','text'],['discount','Discount %','number'],['expiresAt','Expiry','date']].map(([n,l,t])=><label key={n} className="block text-sm font-medium">{l}<input required type={t} value={form[n]} onChange={e=>setForm({...form,[n]:e.target.value})} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5 outline-none focus:ring-2 focus:ring-violet-200"/></label>)}<button className="w-full inline-flex justify-center items-center gap-2 rounded-xl bg-violet-600 text-white py-3 text-sm font-semibold"><Plus size={16}/> Add coupon</button></form><div className="rounded-2xl border border-slate-200 bg-white overflow-hidden"><div className="p-5 border-b border-slate-100"><h2 className="font-semibold">Active codes</h2></div><div className="divide-y divide-slate-100">{coupons.map(c=><div key={c.code} className="p-5 flex flex-col sm:flex-row sm:items-center justify-between gap-3"><div><p className="font-semibold tracking-wide">{c.code}</p><p className="text-xs text-slate-500 mt-1">{c.description}</p></div><div className="flex items-center gap-4"><span className="rounded-full bg-violet-50 text-violet-700 px-3 py-1 text-xs font-semibold">{c.discount}% off</span><button onClick={()=>{setCoupons(x=>x.filter(v=>v.code!==c.code));toast.success('Coupon removed')}} className="text-slate-400 hover:text-red-600"><Trash2 size={17}/></button></div></div>)}</div></div></div></div>}
