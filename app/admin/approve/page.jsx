'use client'
import { storesDummyData } from '@/assets/assets'; import StoreInfo from '@/components/admin/StoreInfo'; import { useState } from 'react'; import toast from 'react-hot-toast'; import { Check, X } from 'lucide-react'
export default function AdminApprove(){const [stores,setStores]=useState(storesDummyData.filter(s=>s.status!=='approved'));const update=(id,status)=>{setStores(s=>s.map(x=>x.id===id?{...x,status}:x));toast.success(`Store ${status}`)};return <div><p className="text-xs font-semibold uppercase tracking-[.2em] text-violet-600">Moderation</p><h1 className="mt-1 text-3xl font-semibold">Store applications</h1><p className="mt-2 text-sm text-slate-500">Review seller information before activation.</p>{stores.length?<div className="mt-7 grid gap-4">{stores.map(store=><div key={store.id} className="rounded-2xl border border-slate-200 bg-white p-5 sm:p-6"><StoreInfo store={store}/><div className="mt-6 flex gap-2"><button onClick={()=>update(store.id,'approved')} className="inline-flex items-center gap-2 rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white"><Check size={16}/> Approve</button><button onClick={()=>update(store.id,'rejected')} className="inline-flex items-center gap-2 rounded-xl bg-slate-100 px-4 py-2.5 text-sm font-semibold text-slate-700"><X size={16}/> Reject</button></div></div>)}</div>:<div className="mt-7 rounded-2xl border border-dashed border-slate-300 p-12 text-center text-slate-400">No pending applications.</div>}</div>}
